//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
//     Manual changes to this file will be overwritten if the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using AutoMapper;
using MediatR;
using WonderKid.DAL.Interface;
using Microsoft.EntityFrameworkCore;
using System.Linq.Expressions;
using Microsoft.Extensions.Logging;
using MIT.ECSR.Data;
using MIT.ECSR.Data.Model;
using MIT.ECSR.Shared.Attributes;
using MIT.ECSR.Core.Response;
using MIT.ECSR.Core.Helper;

namespace MIT.ECSR.Core.Usulan.Query
{
    public class GetUsulanListRequest : ListRequest,IListRequest<GetUsulanListRequest>,IRequest<ListResponse<UsulanResponse>>
    {
    }
    internal class GetUsulanListHandler : IRequestHandler<GetUsulanListRequest, ListResponse<UsulanResponse>>
    {
        private readonly ILogger _logger;
        private readonly IMapper _mapper;
        private readonly IUnitOfWork<ApplicationDBContext> _context;
        public GetUsulanListHandler(
            ILogger<GetUsulanListHandler> logger,
            IMapper mapper,
            IUnitOfWork<ApplicationDBContext> context
            )
        {
            _logger = logger;
            _mapper = mapper;
            _context = context;
        }

        public async Task<ListResponse<UsulanResponse>> Handle(GetUsulanListRequest request, CancellationToken cancellationToken)
        {
            ListResponse<UsulanResponse> result = new ListResponse<UsulanResponse>();
            try
            {
				var query = _context.Entity<MIT.ECSR.Data.Model.TrsUsulan>().AsQueryable();

				#region Filter
				Expression<Func<MIT.ECSR.Data.Model.TrsUsulan, object>> column_sort = null;
				List<Expression<Func<MIT.ECSR.Data.Model.TrsUsulan, bool>>> where = new List<Expression<Func<MIT.ECSR.Data.Model.TrsUsulan, bool>>>();
				if (request.Filter != null && request.Filter.Count > 0)
				{
					foreach (var f in request.Filter)
					{
						var obj = ListExpression(f.Search, f.Field, true);
						if (obj.where != null)
							where.Add(obj.where);
					}
				}
				if (where != null && where.Count() > 0)
				{
					foreach (var w in where)
					{
						query = query.Where(w);
					}
				}
				if (request.Sort != null)
                {
					column_sort = ListExpression(request.Sort.Field, request.Sort.Field, false).order!;
					if(column_sort != null)
						query = request.Sort.Type == SortTypeEnum.ASC ? query.OrderBy(column_sort) : query.OrderByDescending(column_sort);
					else
						query = query.OrderBy(d=>d.Id);
				}
				#endregion

				var query_count = query;
				if (request.Start.HasValue && request.Length.HasValue && request.Length > 0)
					query = query.Skip((request.Start.Value - 1) * request.Length.Value).Take(request.Length.Value);
				var data_list = await query.ToListAsync();

				result.List = _mapper.Map<List<UsulanResponse>>(data_list);
				result.Filtered = data_list.Count();
				result.Count = await query_count.CountAsync();
				result.OK();
            }
            catch (Exception ex)
            {
                _logger.LogError(ex, "Failed Get List Usulan", request);
                result.Error("Failed Get List Usulan", ex.Message);
            }
            return result;
        }

        #region List Utility
		private (Expression<Func<MIT.ECSR.Data.Model.TrsUsulan, bool>> where, Expression<Func<MIT.ECSR.Data.Model.TrsUsulan, object>> order) ListExpression(string search, string field, bool is_where)
		{
			Expression<Func<MIT.ECSR.Data.Model.TrsUsulan, object>> result_order = null;
			Expression<Func<MIT.ECSR.Data.Model.TrsUsulan, bool>> result_where = null;
            if (!string.IsNullOrWhiteSpace(search) && !string.IsNullOrWhiteSpace(field))
            {
                field = field.Trim().ToLower();
                search = search.Trim().ToLower();
                switch (field)
                {
					case "id" : 
						if(is_where){
							if (Guid.TryParse(search, out var _Id))
								result_where = (d=>d.Id == _Id);
						}
						else
							result_order = (d => d.Id);
					break;
					case "approvedat" : 
						if(is_where){
							if (DateTime.TryParse(search, out var _ApprovedAt))
								result_where = (d=>d.ApprovedAt == _ApprovedAt);
						}
						else
							result_order = (d => d.ApprovedAt);
					break;
					case "approvedby" : 
						if(is_where){
							result_where = (d=>d.ApprovedBy.Trim().ToLower().Contains(search));
						}
						else
							result_order = (d => d.ApprovedBy);
					break;
					case "createby" : 
						if(is_where){
							result_where = (d=>d.CreateBy.Trim().ToLower().Contains(search));
						}
						else
							result_order = (d => d.CreateBy);
					break;
					case "createdate" : 
						if(is_where){
							if (DateTime.TryParse(search, out var _CreateDate))
								result_where = (d=>d.CreateDate == _CreateDate);
						}
						else
							result_order = (d => d.CreateDate);
					break;
					case "deskripsi" : 
						if(is_where){
							result_where = (d=>d.Deskripsi.Trim().ToLower().Contains(search));
						}
						else
							result_order = (d => d.Deskripsi);
					break;
					case "endtglpelaksanaan" : 
						if(is_where){
							if (DateTime.TryParse(search, out var _EndTglPelaksanaan))
								result_where = (d=>d.EndTglPelaksanaan == _EndTglPelaksanaan);
						}
						else
							result_order = (d => d.EndTglPelaksanaan);
					break;
					case "idjenisprogram" : 
						if(is_where){
							if (int.TryParse(search, out var _IdJenisProgram))
								result_where = (d=>d.IdJenisProgram == _IdJenisProgram);
						}
						else
							result_order = (d => d.IdJenisProgram);
					break;
					case "idperusahaan" : 
						if(is_where){
							if (Guid.TryParse(search, out var _IdPerusahaan))
								result_where = (d=>d.IdPerusahaan == _IdPerusahaan);
						}
						else
							result_order = (d => d.IdPerusahaan);
					break;
					case "lokasi" : 
						if(is_where){
							result_where = (d=>d.Lokasi.Trim().ToLower().Contains(search));
						}
						else
							result_order = (d => d.Lokasi);
					break;
					case "namaprogram" : 
						if(is_where){
							result_where = (d=>d.NamaProgram.Trim().ToLower().Contains(search));
						}
						else
							result_order = (d => d.NamaProgram);
					break;
					case "notes" : 
						if(is_where){
							result_where = (d=>d.Notes.Trim().ToLower().Contains(search));
						}
						else
							result_order = (d => d.Notes);
					break;
					case "starttglpelaksanaan" : 
						if(is_where){
							if (DateTime.TryParse(search, out var _StartTglPelaksanaan))
								result_where = (d=>d.StartTglPelaksanaan == _StartTglPelaksanaan);
						}
						else
							result_order = (d => d.StartTglPelaksanaan);
					break;
					case "status" : 
						if(is_where){
							if (short.TryParse(search, out var _Status))
								result_where = (d=>d.Status == _Status);
						}
						else
							result_order = (d => d.Status);
					break;

                }
            }
            return (result_where, result_order);
        }
        #endregion
    }
}

